package com.synapse.core.designsystem

import androidx.compose.ui.graphics.Color

// ─── Background hierarchy ────────────────────────────────────────────────────
val BackgroundDeep = Color(0xFF070710)       // deepest bg – main screen bg
val BackgroundSurface = Color(0xFF0D0D1A)    // cards, sheets
val BackgroundElevated = Color(0xFF14142B)   // elevated surfaces
val BackgroundOverlay = Color(0xB2070710)    // semi-transparent overlays

// ─── Border / Divider ────────────────────────────────────────────────────────
val BorderDefault = Color(0xFF1E1E35)
val BorderSubtle = Color(0xFF16162A)
val BorderBright = Color(0xFF2E2E55)

// ─── Brand / Primary accent ──────────────────────────────────────────────────
val BrandLime = Color(0xFFB4FF00)            // neon lime – primary brand (image 1)
val BrandLimeDim = Color(0xFF7AAB00)
val BrandLimeContainer = Color(0xFF1A2400)

// ─── Skeleton / Sensor visualization ─────────────────────────────────────────
val SkeletonCyan = Color(0xFF00E5CC)         // turquoise joints (image 3)
val SkeletonCyanGlow = Color(0x5500E5CC)
val SkeletonGood = Color(0xFF00FF88)         // correct technique
val SkeletonWarn = Color(0xFFFFB800)         // minor deviation
val SkeletonError = Color(0xFFFF6B3D)        // significant deviation
val SkeletonDanger = Color(0xFFFF3355)       // dangerous movement

// ─── Semantic ────────────────────────────────────────────────────────────────
val Success = Color(0xFF00CC6A)
val Warning = Color(0xFFFFB800)
val Error = Color(0xFFFF3355)
val Info = Color(0xFF4488FF)

// ─── Text ────────────────────────────────────────────────────────────────────
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFF9999BB)
val TextDisabled = Color(0xFF444460)
val TextOnBrand = Color(0xFF070710)

// ─── Misc ────────────────────────────────────────────────────────────────────
val AccentBlue = Color(0xFF3D7BFF)
val AccentPurple = Color(0xFF8855FF)
val Transparent = Color(0x00000000)
